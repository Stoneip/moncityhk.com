---
const steps = [
  { num: '1', title: 'WhatsApp 查詢', desc: '透過 WhatsApp 告知需要更換嘅零件型號，我哋即時回覆報價。' },
  { num: '2', title: '到店選購', desc: '帶手機到深水埗深之都門市，即場確認零件及安裝方案。' },
  { num: '3', title: '即場安裝', desc: '確認後即場安裝更換，大部分零件即日完成。' },
];
---

<section class="section process-section" id="process">
  <div class="container">
    <h2 class="section-title">服務流程</h2>
    <p class="section-subtitle">簡單三步，即場更換</p>
    <div class="process-steps">
      {steps.map((step, i) => (
        <div class="step">
          <div class="step-num">{step.num}</div>
          <h3 class="step-title">{step.title}</h3>
          <p class="step-desc">{step.desc}</p>
          {i < steps.length - 1 && <div class="step-arrow" aria-hidden="true">→</div>}
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .process-section {
    background: var(--primary);
  }
  .process-section .section-title {
    color: #fff;
  }
  .process-section .section-subtitle {
    color: rgba(255,255,255,0.75);
  }
  .process-steps {
    display: flex;
    align-items: flex-start;
    gap: 24px;
    position: relative;
  }
  .step {
    flex: 1;
    text-align: center;
    position: relative;
  }
  .step-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: #fff;
    color: var(--primary);
    font-weight: 700;
    font-size: 1.2rem;
    border-radius: 50%;
    margin-bottom: 16px;
  }
  .step-title { font-size: 1.1rem; font-weight: 700; margin-bottom: 8px; color: #fff; }
  .step-desc { color: rgba(255,255,255,0.8); font-size: 0.95rem; }
  .step-arrow {
    position: absolute;
    right: -24px;
    top: 24px;
    font-size: 1.5rem;
    color: rgba(255,255,255,0.6);
    font-weight: 700;
  }

  @media (max-width: 768px) {
    .process-steps { flex-direction: column; align-items: center; }
    .step-arrow { display: none; }
    .step { max-width: 400px; }
  }
</style>
