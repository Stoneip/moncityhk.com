---
const stats = [
  { number: '750+', label: '型號零件' },
  { number: '15+', label: '品牌覆蓋' },
  { number: '原裝/副廠', label: '雙選擇' },
  { number: '90日', label: '零件保養' },
];
---

<section class="stats">
  <div class="container stats-grid">
    {stats.map(s => (
      <div class="stat-item">
        <span class="stat-number">{s.number}</span>
        <span class="stat-label">{s.label}</span>
      </div>
    ))}
  </div>
</section>

<style>
  .stats {
    background: var(--primary);
    padding: 32px 0;
  }
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
    text-align: center;
  }
  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .stat-number {
    font-size: 2rem;
    font-weight: 800;
    color: #fff;
  }
  .stat-label {
    font-size: 0.95rem;
    color: rgba(255,255,255,0.85);
  }

  @media (max-width: 768px) {
    .stats-grid { grid-template-columns: repeat(2, 1fr); gap: 20px; }
    .stat-number { font-size: 1.6rem; }
  }
</style>
